input_str = input()

for c in input_str:
    if ord(c) == 92:
        print()
        continue
    print(c, end='')

# Правильный вариант:

path = input('Введите полное имя файла: ')
for part in path.split('/'):
    print(part)

def max_digits():
    s = input()
    num_list = []
    num_list = s.split()
    for i in range(len(num_list)):
        num = int(num_list[i])
        num_list[i] = num
    max_digit = max(num_list)
    return max_digit

print(max_digits() * max_digits())
